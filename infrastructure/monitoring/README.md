# Monitoring

Prometheus/Grafana and tracing deployment assets are deliberately deferred. The current services establish health, request ID, and structured logging conventions that later collectors can consume.
