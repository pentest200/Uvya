{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://uvya.dev/contracts/events/v1/message.read.v1.schema.json",
  "title": "Uvya message.read v1",
  "allOf": [
    { "$ref": "./event-envelope.v1.schema.json" },
    { "properties": { "eventType": { "const": "message.read" }, "payload": { "type": "object", "required": ["chatId", "userId", "lastReadSequence"], "properties": { "chatId": { "type": "string", "format": "uuid" }, "userId": { "type": "string", "format": "uuid" }, "lastReadSequence": { "type": "integer", "minimum": 0 } } } }, "required": ["eventType", "payload"] }
  ]
}
